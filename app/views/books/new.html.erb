<h1>New book</h1>

<%= form_with model: @book, url: book_path(@book.id), method: :post do |f| %>


<h4>Title</h4>
<%= f.text_field :title %>

<h4>body</h4>
<%= f.text_area :body %>

<br><%= f.submit 'Create Book' %></br>

<%= link_to "show", book_path(@book.id) %>

<% end %>

